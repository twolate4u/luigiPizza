package de.luigi.pizza;

public class Cal {
 }


/*
    private double costPizzaBasis = 4.99; // Basiskosten

    //Arry für Zutat * preis

    int [] arryZutat = Zutat int [8];
    for (int i = 0; i<8;i++)
    {
    arryZutat[i] = (i*cost);

double costZutat = cost



    double totalCost; //variable to hold the sum of total costs
            totalCost = 0.00;
            totalCost += pizza.getCost();
    for (int i = 0;  i < price.size(); i++){

        totalPrices += price.get(i);
    }
    System.out.println("All Totals are: " + totalPrices);
        for (Pizza pizza : orderList) {
            totalCost += pizza.getCost();
        }




    public void totalOrderCost() {
        totalOrderCost = costPizzaBasis + costSauce + costZutat;


double total sauce;
total sauce += zutat.getWahl()* zutat.getCost

double total zutat;
total Zutat += zutat.getWahl()* zutat.getCost

}

System.out.println(orderName + );
// Creating PizzaOrder object and relevant information with regards to order */